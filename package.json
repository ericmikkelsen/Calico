{
  "name": "calico",
  "version": "1.0.0",
  "description": "Generates color information about color palette",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && mkdir public && mkdir public/palettes && node --experimental-modules index.mjs && npm run palette && npm run copy:assets",
    "clean": "rm -rf ./public",
    "copy:assets": "cp -a ./modules/ ./public/ && cp -a ./styles/ ./public/",
    "palette": "node --experimental-modules palette.mjs",
    "serve": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ericmikkelsen/Calico.git"
  },
  "keywords": [
    "color",
    "a11y",
    "accessibility"
  ],
  "author": "Eric Mikkelsen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ericmikkelsen/Calico/issues"
  },
  "homepage": "https://github.com/ericmikkelsen/Calico#readme",
  "dependencies": {}
}
